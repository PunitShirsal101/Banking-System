package com.spunit.common.security;

/**
 * Placeholder for shared security configuration. Intentionally left empty to avoid coupling common module
 * to Spring Security. Each service and the gateway will declare its own security config.
 */
public final class CommonResourceServerAutoConfiguration {
    private CommonResourceServerAutoConfiguration() {}
}
